pnpm check

# Regenerate frameworks manifest if any framework files changed (components or patterns)
if git diff --cached --name-only | grep -qE "docs/(components|patterns)/.*/frameworks/.*\.md$"; then
  echo "Framework files changed, regenerating manifest..."
  pnpm generate-frameworks
  git add docs/.vitepress/frameworks-manifest.json
fi