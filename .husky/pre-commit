pnpm check

# Regenerate frameworks manifest if any framework files changed
if git diff --cached --name-only | grep -q "docs/components/.*/frameworks/.*\.md$"; then
  echo "Framework files changed, regenerating manifest..."
  pnpm generate-frameworks
  git add docs/.vitepress/frameworks-manifest.json
fi